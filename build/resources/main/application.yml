management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - metrics
          - prometheus
  tracing:
    enabled: true
    sampling.probability: 1.0
    otlp.tracing:
      endpoint: http://tempo:4317
      protocol: grpc

server:
  tomcat:
    mbeanregistry:
      enabled: true